import React from "react";
import * as Styles from "../Logo/styles";

const Logo = () => {
  return (
    <Styles.Logo>
      <img src="../img/logo.png" alt="Logomarca" />
    </Styles.Logo>
  );
};

export default Logo;
